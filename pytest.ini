[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    integration: тесты с PostgreSQL (docker-compose up postgres)
